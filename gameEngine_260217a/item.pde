class Item {
  // Variables
  float w = 200;
  float h = 520;
  float x;
  float y;
  color c;
  
  // Constructor
  Item(float x, float y) {
    this.x = x;
    this.y = y;
  }
  
  // Methods
  void display() {
    for (frameCount == 
    if (key == 's') {
      c = #0078FF;
    } else {
      c = #FF0000;
    }
    fill(c);
    rect(x, y, w, h);
  }
  // select()
  // place()
  // clear()
  // random()
}
